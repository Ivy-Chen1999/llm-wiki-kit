# More on vector databases
Vector databases commonly use HNSW or IVF indexes to make approximate nearest-neighbor search
fast at scale. They are the first-stage retriever in most RAG pipelines. Popular examples include
FAISS-backed stores and managed services.
