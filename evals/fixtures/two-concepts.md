# Notes on two ideas

## Vector databases
A vector database stores embeddings and supports approximate nearest-neighbor search.
Used for semantic retrieval.

## Reranking
Reranking reorders an initial candidate list with a stronger model to improve precision.
Often applied after a vector-database first-stage retrieval.
